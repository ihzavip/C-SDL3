# Project Scope

- Only modify files under `topdown/` — this is the active codebase
- Do not modify `game/` — separate terminal roguelike, not under active development
- Do not modify `src/sdl_hello.c` — superseded SDL3 prototype, ignore it
